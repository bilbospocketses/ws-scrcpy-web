# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- `feat(sp3):` SP3 P3 — service mode (Windows). New cross-platform `ServiceClient` interface (`src/server/service/ServiceClient.ts`) + `ServyClient` Windows implementation (`src/server/service/ServyClient.ts`, backed by Servy v8.2 CLI bundled into `publish/servy-cli.exe`) + `SystemdClient` Linux stub (every method throws — full implementation lands in a later SP3 sub-phase) + factory at `src/server/service/index.ts`. Servy bundling via `scripts/fetch-servy.mjs` (pinned v8.2; downloads `servy-8.2-x64-portable.7z` from GitHub Releases, SHA256-verifies the archive, extracts via Windows `tar.exe`/bsdtar, SHA256-verifies the inner `servy-cli.exe`, copies to `publish/servy-cli.exe`); wired into `scripts/stage-publish.mjs`. New `src/server/InstallScope.ts` detects PerUser vs PerMachine from `process.execPath` against `LOCALAPPDATA`. New `src/server/api/ServiceApi.ts` exposes `GET /api/service/status` (returns `{ supported, platform, status?, unsupportedReason? }`), `POST /api/service/install` (auto-detects scope → `currentUser` for user / `LocalSystem` for machine → calls `client.install` → persists `installMode='user-service'` or `'system-service'`), `POST /api/service/uninstall` (best-effort stop + uninstall + revert `installMode`). New shared types in `src/common/ServiceEvents.ts` plus canonical service-name constants (`WS_SCRCPY_SERVICE_NAME='WsScrcpyWeb'`, etc.). Frontend: new `src/app/client/SettingsModal.ts` (extends Modal base; sections for Server/Updates-stub/Service/App-stub; Service section fetches `/api/service/status` on open, renders Install/Uninstall buttons + status indicator + Linux unsupported notice + graceful error+retry UX); new `src/app/client/SettingsHeader.ts` (gear icon mounted next to ThemeToggle); CSS additions to `src/style/modal.css` (settings sections) and `src/style/home.css` (`.settings-header`). WelcomeModal back-fill (T3.7): "Yes, install service" now first checks `/api/service/status` for platform support, then either calls `POST /api/service/install` end-to-end or falls back gracefully on Linux. +34 tests (vitest 429 total, up from 395). Architectural decision logged: Linux Velopack + systemd service mode are **in-scope for SP3** as a later sub-phase before SP3 closes — the `ServiceClient` interface keeps the Linux phase purely additive. Lead-validation findings (live smoke surfaced two real bugs, both fixed): missing `await` on inner handler dispatches in `ServiceApi.handle()` and webpack-bundled-runtime path resolution in `ServyClient.resolveServyPath()` (added `cwd/publish/servy-cli.exe` as primary dev-mode candidate). Servy v8.2 first-run quirk to flag in P5/P7 docs: even read-only `servy-cli list` requires Administrator elevation on first run because Servy auto-registers a Windows Event Log source.
- `feat(sp3):` SP3 P2 — Velopack hooks + lifecycle scaffolding. `config.json` schema (`installMode`, `firstRunComplete`, `autoUpdate`, `updateCheckIntervalMinutes`, `channel`, `githubOwner`, `webPort`) lives at install root and survives updates; legacy `port` migrates to `webPort` in memory only (no surprise file rewrites). New `src/server/PortPicker.ts` finds an open port in a range; `src/server/index.ts` reconciles `webPort` against actual availability on every startup, persists shifted port, and surfaces `portWasAutoShifted` on the runtime envelope. New `src/server/api/ConfigApi.ts` exposes `GET /api/config` (returns `{ config, runtime: { firstRunComplete, portWasAutoShifted, webPort } }`) and `PATCH /api/config` (validates partial updates, returns `{ config, restartRequired }` where `restartRequired=true` when `webPort` changes). `src/common/ConfigEvents.ts` is the shared type surface. `velopack` npm package wired and `VelopackApp.build().run()` is the very first executable statement in `src/server/index.ts` (Contract 5 cross-cutter). Rust launcher (`launcher/src/hooks.rs` + `launcher/src/config.rs`) parses `--veloapp-install` / `--veloapp-updated` / `--veloapp-uninstall` argv flags and dispatches before `velopack::VelopackApp::build().run()`; Rust crate `velopack 0.0.1298` does not auto-dispatch lifecycle hooks (Rust SDK is more minimal than C#), so we handle them ourselves. Servy-cli.exe absent in P2 → log + exit 0 (deferred to P3). User data preserved on uninstall. First-run welcome modal (`src/app/client/WelcomeModal.ts`, extends the existing `Modal` base class) auto-mounts in `src/app/index.ts` when `runtime.firstRunComplete=false`; "Yes, install service" / "No, run on demand" buttons PATCH `installMode` + `firstRunComplete=true` (real service registration deferred to P3). +50 tests (cargo: 13 hooks + 7 config; vitest: 13 Config + 4 PortPicker + 13 ConfigApi); 395 vitest cases passing total.
- `feat:` Quick Scan button on the home page — runs mDNS-only discovery to find modern Android devices advertising over `_adb-tls-connect._tcp.` without probing every host on a subnet. Uses the same `/ws-scan` WebSocket pipeline as full scan; `ScanStartMessage` gained an optional `mdnsOnly?: boolean` flag and `NetworkScanner.start()` accepts an `{ mdnsOnly }` option that short-circuits the TCP worker pool. UI uses inline status text instead of the progress chip since mDNS resolves in ~1s. Restores a convenience that was lost when the full TCP+mDNS scanner replaced the simpler pre-rewrite flow.

### Fixed
- `fix:` Embed API codec parameter now validated against browser capabilities before connecting. Previously, passing `codec: 'h265'` on Firefox (which cannot decode HEVC) would bomb the stream — the `browserSupportsCodec()` check only ran when no codec was specified (auto-detect path). Now explicitly-supplied codecs are verified via `VideoDecoder.isConfigSupported()` in `StreamClientScrcpy.startStream()` and fall back to h264 if unsupported. The caller-supplied encoder is also cleared on fallback since it was chosen for the original codec.

### Added
- `sdkInt: number` field in `ProbeResult` interface — `DeviceProbe` now includes the device's Android SDK version (from `ro.build.version.sdk`) in the WebSocket probe response. The value was already computed internally for encoder-enumeration strategy selection; exposing it avoids downstream consumers (e.g. Control Menu's audio settings gating) needing a redundant `adb shell getprop` call.

### Changed
- `node-pty` native dependency handled via our own prebuilt matrix + a two-source resolver, no `node-gyp rebuild` at install time. Shipped in two steps: SP1 (April 2026) introduced a GH Actions matrix that builds `microsoft/node-pty` from source across platforms and publishes tarballs to GH Releases; SP1b (April 2026) dropped the `@homebridge/node-pty-prebuilt-multiarch` fork indirection and made our own prebuilts authoritative. Current layout: `node-pty@^1.1.0` is declared under `optionalDependencies`; `.npmrc` sets `ignore-scripts=true` globally so the package install doesn't trigger a native compile. The resolver downloads + extracts binaries on first run. SP1+SP1b are step 1 of the installer + Docker roadmap (`docs/superpowers/specs/2026-04-21-installer-docker-roadmap.md`).

### Added
- `src/server/NodePtyResolver.ts` — runtime resolution chain for node-pty. Two sources: (1) **local cache** at `dependencies/node-pty/v{upstreamVersion}/{platform}-{arch}[-{libc}]/` populated by a prior run / installer / `npm run fetch-prebuilts`, (2) **download** from our GitHub Releases (`node-pty-prebuilds-latest` manifest → `node-pty-prebuilds-v{upstream}` tarball) with SHA256 verification against `SHA256SUMS`. After either source resolves, the cache contents are copied to `node_modules/node-pty/build/Release/` and upstream node-pty's standardized `lib/utils.js` loader finds the binary via its `build/Release/` + `prebuilds/{platform}-{arch}/` iteration — identical behavior on Linux, macOS, and Windows. Failure modes surface as typed `reason` strings (`no-manifest`, `no-prebuilt-for-abi-...`, `download-failed`, `copy-failed`, `import-invalid`, `import-failed`) for `CapabilitiesApi` graceful degradation.
- `scripts/fetch-prebuilts.mjs` — pure-JS CLI (no TS compile dependency) that fetches the current host's prebuilt and places it in `node_modules/node-pty/build/Release/`. Wired as `npm run fetch-prebuilts`. Used for air-gapped dev setups and CI pre-test binary warming.
- `vitest.globalSetup.ts` — invokes the fetch script before any test run, ensuring `node_modules/node-pty/build/Release/pty.node` exists for happy-path tests and for the integration test's fixture generation.
- `src/server/__tests__/nodePtyResolver.integration.test.ts` — end-to-end download-path test with a local HTTP fixture server. Three cases: happy path (download → extract → place → require), cache-hit skipping on second call, `download-failed` reason when checksum is wrong. Closes the integration-test gap the SP1 reviewer flagged.
- `src/server/__tests__/setup-fixture.ts` — helper that packs a directory into a tarball named after the prebuilt key, mirroring the matrix workflow's archive format (for the integration test).
- `.npmrc` — sets `ignore-scripts=true` globally.
- `src/server/libcDetect.ts` — detects glibc vs musl on Linux via three probes (`process.report.header.glibcVersionRuntime`, `/etc/alpine-release` presence, `ldd --version` stderr match). Used by the resolver to pick the right prebuilt for Alpine vs Debian-family containers. Always returns `'glibc'` on win32.
- `src/server/api/CapabilitiesApi.ts` — new `GET /api/capabilities` endpoint returning `{ shell: boolean }` based on whether the resolver successfully loaded node-pty. Exposed so the frontend can gracefully degrade when shell is unavailable.
- Shell button on each device card in the Connected Devices section now checks `/api/capabilities` on mount. When `shell: false`, the anchor gets `aria-disabled="true"`, `pointer-events: none`, muted opacity, and a tooltip explaining the node-pty unavailability. Other features (stream, file browser, scan, connect, sleep/wake) work normally — the shell feature is the only thing that requires node-pty.
- `.github/workflows/node-pty-prebuilds.yml` — weekly Monday 09:00 UTC + manual dispatch. A pre-check job compares tracked state in `.github/state/node-pty-prebuilds-state.json` against the latest Node LTS list from `nodejs.org/dist/index.json` and the latest `microsoft/node-pty` upstream release. On any change, a 10-row matrix builds prebuilts for `{win32 x64, win32 arm64, linux x64 glibc, linux arm64 glibc, linux x64 musl} × {current LTS, prior LTS}`. (`linux arm64 musl` is intentionally excluded: GitHub Actions' JS-action runtime can't execute inside an Alpine container on an ARM64 runner — actions/runner#801. Hosts on arm64+musl fall through the resolver chain to compile-at-install via the bundled @homebridge package.) A publish job attaches the tarballs + `SHA256SUMS` + `manifest.json` to a versioned GitHub Release tagged `node-pty-prebuilds-v{upstream}`, and updates a `node-pty-prebuilds-latest` release whose only asset is the manifest (the consumer resolver uses this to discover the upstream version to download). A failure job auto-opens a GitHub issue tagged `prebuild-failure` with a link to the failing run.
- `scripts/compute-matrix-versions.mjs` — Node script that the workflow's pre-check step runs. Emits `{changed, fresh, previous}` JSON and uses exit codes (0 = no changes / no-op, 1 = changes detected / run the matrix, 2 = error) to signal the workflow.

### Internal
- Dead-code sweep (2026-04-18): removed five verified-orphan files (`src/app/interactionHandler/SimpleInteractionHandler.ts`, `src/app/googDevice/DragAndPushLogger.ts`, `src/common/WdaStatus.ts`, `src/types/MessageFileListing.d.ts`, `src/types/ReplyFileListing.d.ts`) and the unused `.flex-center` rule from `src/style/app.css`. `docs/TECHNICAL_GUIDE.md` dropped its `SimpleInteractionHandler` mention; historical plan docs intentionally unchanged. `MediaFormat.ts` kept — ~700 lines of JSDoc documenting Android's `MediaFormat` key constants, zero runtime cost (tree-shaken), useful reference if we ever surface codec-capability UI. -243 lines net.
- Replaced Util's Google-Closure-vintage UTF-8 codec shims (`stringToUtf8ByteArray` / `utf8ByteArrayToString`, 2008 Apache 2.0, still carrying the `TODO(user): Use native implementations if/when available` comment) with native `TextEncoder` / `TextDecoder` across 20 call sites in 9 files. `Util.ts` lost 82 lines; 5 files dropped their `import Util` entirely. Behaviorally equivalent — both produce UTF-8 per spec — but WHATWG `TextDecoder` is more correct on malformed input and Node 14+ / all browsers have had the natives for years.
- Narrowed `ParamsDeviceTracker.type` from `'android' | 'ios'` to `'android'`. iOS support was intentionally dropped long before this fork (related to `WdaStatus.ts`, also removed in this sweep). `BaseDeviceTracker.parseParameters` lost its dead `'ios'` acceptance branch.
- Dropped `Event2` / `CloseEvent2` / `MessageEvent2` polyfills from `src/packages/multiplexer/` — Node 24 (repo `engines` floor) has all three as globals, and the runtime `typeof` check always picked native on any supported runtime. Inlined `new Event(…)` / `new CloseEvent(…)` / `new MessageEvent(…)` at the five Multiplexer and Message call sites. Kept `ErrorEventClass.ts` but rewrote it to extend native `Event` instead of the deleted `Event2` — Node 24 still does NOT define `ErrorEvent` as a global (tracked at nodejs/node#49986; internal-only in undici). Runtime smoke test confirmed the polyfill dispatches correctly with all browser `ErrorEvent` fields (`error`, `message`, `filename`, `lineno`, `colno`). -137 / +18 across 6 files.
- New `src/types/assets.d.ts` declares ambient modules for webpack-loaded side-effect imports (`*.css`, `*.svg`, `*.png`, `*.jpg`, `*.gif`, `*/assets/scrcpy-server`) plus the DefinePlugin build-time constants `__PATHNAME__` and `__WSSCRCPY_VERSION__`. The standalone `tsc` checker and `dts-bundle-generator` can now resolve these imports without error. Drops the TS pre-emit diagnostic count from **78 → 43** (phase 1a of the TS cleanup initiative — clears all TS2307, TS2882, and TS2552 errors)
- Phase 1b one-off fixes (**43 → 35**): inline `[DeviceProbe]` / `[ScrcpyConnection]` tag strings where a missing module-level `const TAG` referenced undefined names; `import type { BasePlayer }` (named) in `ConnectModal.ts` instead of default; `import { WebSocketServer as WSServer }` in `WebSocketServer.ts` (the `ws` package type defs export `Server` as type-only, the value class is `WebSocketServer`); optional chaining on `os.networkInterfaces()` iterator in `Utils.ts` (the dict values are possibly undefined); widened `Omit<Required<ScrcpyOptions>, 'scid' \| 'videoEncoder'>` so the serializer doesn't demand a default for the always-optional `videoEncoder` field; cast-through-unknown in the same file's dict indexer; second `@ts-expect-error` directive in `Mw.ts` for the `WS \| Multiplexer` union's second `addEventListener` call (real fix comes in phase 1e)
- Phase 1c `HostItem` shape drift (**35 → 32**): `BaseDeviceTracker.buildUrl()` was declared to take a full `HostItem` but only reads `secure`/`hostname`/`port`/`pathname`. Three call sites passed narrower object literals (either missing `type` or with a widened `type: 'android' \| 'ios'` from `ParamsDeviceTracker`) and tripped `TS2345`. Narrowed the `buildUrl` parameter to an inline structural type matching exactly what the function consumes — `HostItem` is still accepted (it's a superset), and the narrower call sites now type-check cleanly
- Phase 1d xterm/node-pty API drift + stale method (**32 → 27**): `FitAddon.proposeDimensions()` now returns `ITerminalDimensions \| undefined` — `ShellClient.startShell()` destructured it directly, added a `?? 24` / `?? 80` fallback matching `ShellModal.startShell()`'s existing pattern; `RemoteShell.createTerminal` used legacy `term.on('data'/'exit', ...)` — node-pty 1.x switched to `term.onData()` and `term.onExit({exitCode, signal})`, updated; `ShellModal.sendResize()` had the outer message wrapper `type: 'resize'` but `MessageXtermClient` requires `type: 'shell'` (the inner `data.type` is where the `'resize'` action lives) — fixed to `type: 'shell'`; `DeviceTracker.updateLink()` had a dead `if (store)` branch referencing a nonexistent `DeviceTracker.getLocalStorageKey` method — removed the branch and the unused `store` parameter
- Phase 1f retired the `scripts/build-types.js` monkey-patch that swallowed pre-emit diagnostics in dts-bundle-generator. Root cause: the tool calls `ts.parseJsonConfigFileContent` but throws away its `fileNames` array, so tsconfig's `include` glob is ignored and only the entry file + its transitive imports get loaded — ambient declarations in `src/types/assets.d.ts` and the `@types/node` globals were unreachable even though `tsc --noEmit` saw them fine. Fix: two `/// <reference>` directives at the top of `src/app/public/index.ts` (`path="../../types/assets.d.ts"` and `types="node"`) force dts-bundle-generator's internal TS program to load them. The generated `.d.ts` doesn't leak the references (ambient `declare module '*.css'` isn't reachable from any exported type). `build-types.js` is now a thin 30-line wrapper around `generateDtsBundle` with no monkey-patch; any future pre-emit diagnostic will fail the build loudly instead of being silently swallowed.
- Phase 1e Multiplexer hairball (**27 → 0**, four commits, zero webpack regressions): **1e-α** narrowed `Event2.NONE` / `CAPTURING_PHASE` / `AT_TARGET` / `BUBBLING_PHASE` from `: number` to literal `as const` so `CloseEvent2` / `ErrorEvent2` / `MessageEvent2` structurally match the lib `Event` hierarchy (TS 5.7+ tightened those constants to literal `0|1|2|3` instead of `number`); polyfill kept because Node < 23 still lacks `CloseEvent` / `ErrorEvent` globals. Cleared 3 × TS2420 plus 7 cascade errors. **1e-β** one-line narrow of `Multiplexer.readyState: number` → `0 \| 1 \| 2 \| 3` so `implements WebSocket` holds; cleared TS2416 plus 4 downstream "`Multiplexer` not assignable to `WebSocket`" cascades at ShellClient, ShellModal, FileListing, and `Multiplexer._createChannel`. **1e-γ** widened `Message.createBuffer` to accept `Uint8Array \| ArrayBuffer` and return `Uint8Array<ArrayBuffer>` (runtime already used the `new Uint8Array(data)` constructor which handles both), and cast to `string \| BufferSource \| Blob` at `Multiplexer._send`'s native-`ws.send` boundary while keeping the public `send` / `sendData` / `storage` API at its wider legacy shape so `BinaryWriter.toUint8Array()` callers at AdbkitFilePushStream keep working. **1e-δ** six one-line consumer fixes: `File(chunks as BlobPart[])` at FileListingClient + ListFilesModal, `ws.send(msg as BufferSource)` at ScrcpyDemuxer, explicit `buffer: Buffer` annotation at FrameReader so `Buffer.concat`'s `Buffer<ArrayBufferLike>` result reassigns cleanly, `as unknown as BufferSource` at FilePushReader.sendResponse, and `encoding?: BufferEncoding` (tightened from `string`) at ReadStream.push
- `.dep-btn` (scan network, check for updates) now sets `font-family: var(--font-mono)` explicitly. Like other `<button>` elements it doesn't inherit `font-family` from parent by default — the UA default (sans-serif) was rendering with different character tracking than the device card pills
- Docs refreshed after the port-5555 scanner rewrite: `README.md` network-discovery line now covers the mDNS + TCP-sweep architecture, gateway auto-detect, manual-add, and mDNS/TCP dedupe. `docs/TECHNICAL_GUIDE.md` §14.2 "Available Network Devices" is fully rewritten — six subsections covering the user flow, HTTP endpoints, `/ws-scan` message schema, server-side components (`NetworkScanner` / `AdbHandshakeProbe` / `SubnetDetector` / `MacResolver` / `SubnetParser`), client-side components (`ScanNetworkModal` / `AddSubnetModal` / `LargeSubnetWarningModal` / `ScanProgressChip` / `NetworkDiscoveryPanel`), config tuning knobs, and diagnostic scripts. §16 "Device Labels" expanded to document the dual-key (serial + MAC) persistence scheme and the TCP-hit serial placeholder pattern (real `ro.serialno` is fetched only on user Connect click, not during the scan).
- Browser tab title is now static **"Android Power Tools"** on every page state. Previously it changed to "Device list" on the home page and to "Stream {model}" / "Shell {udid}" / "Listing {serial}" inside their respective views, but never reverted on close — disconnecting from a stream left the title stuck on that device's name until refresh. Removed all four `setTitle()` callers (`BaseDeviceTracker` constructor, `StreamClientScrcpy.onMetadata`, `ShellClient`, `FileListingClient`), deleted the `BaseClient.setTitle()` method and `protected title` field that backed them, and hard-set the static title in `public/index.html`. Net deletion: -14 lines / +1 line.
- ConfigureScrcpy modal grew an **audio settings group** below max-fps: `audio codec` dropdown (existing, relocated), `audio source` dropdown (new — `output` / `playback` / `mic`), `enable audio` checkbox (new). Reordered controls into video-then-audio-then-bitrate groups (bitrate moved last since it covers all channels). Modal max width bumped 650px → 700px to fit the longest source label. Audio-source `playback` option is hidden in the dropdown on Android 12 and older (`--audio-dup` requires Android 13+, so the playback choice would silently degrade to "silences device" without it). Enable-audio checkbox is fully disabled with a tooltip on Android 10 and older where scrcpy can't capture audio at all. Audio source default = `output` everywhere, matching scrcpy's own upstream default; users opt into `playback` (keeps device audio playing during capture) when they want it.

### Added
- Audio capture is now device-kind + SDK aware, with a per-device user toggle and persistence. Three new shared helpers in `src/common/AudioDefaults.ts` (`audioCaptureSupported`, `audioDupSupported`, `defaultAudioSourceForSdk`) drive both client and server gating from a single source of truth. ConfigureScrcpy reads the device's `ro.build.version.sdk` to pick the right defaults, hide options that can't work, and disable controls entirely on devices below the floor. Three audio dropdown options when supported (`output` / `playback` / `mic`), each with a one-line note in the option label. New `AudioSettingsStore` (`src/app/client/AudioSettingsStore.ts`) persists `{ enabled, source, codec }` per-udid in `localStorage` under `ws-scrcpy-web:audio:<udid>` — applied automatically when `ConfigureScrcpy` opens, written when the `save` button is clicked, and loaded on the device card's `connect` button so the streaming flow respects the user's choice without going through the configure dialog every time. Eight new test files cover the behavior: `audioDefaults.test.ts` (12), `audioSettingsStore.test.ts` (9), `streamUrlParams.test.ts` (10), `scrcpyOptions.test.ts` (9), `scrcpyOptionsFromQuery.test.ts` (14), and additions to `embedEntry.test.ts` and `startStream.test.ts`. Server-side, `ScrcpyOptions` gained `audioSource` + `audioDup` fields; `scrcpyOptionsFromQuery` (extracted from `ScrcpyConnection.buildOptions` for testability) parses `audio` / `audioSource` URL params; and `ScrcpyConnection.start()` enforces SDK gates (force-off on SDK<30; force-off when source=playback on SDK<33). Public `StartStreamOptions` API now accepts `audioSource` and `audioCodec`; `embed.html` URL params accept the same; `docs/TECHNICAL_GUIDE.md` §6.6 updated.
- Network scan port-5555 sweep alongside mDNS discovery — finds older Android devices that don't advertise over mDNS (typically pre-Android 11 tablets with wireless debugging enabled on a fixed port). `ScanNetworkModal` is the primary configuration dialog: shows the auto-detected gateway subnet, lets the user add/remove additional subnets (persisted in `localStorage`), with live CIDR/IP/range validation. Accepts bare IPs, CIDR (`/n`), and IP ranges (`a.b.c.d-e`) per line. Scan runs over a new `/ws-scan` WebSocket endpoint streaming `scan.start` / `scan.progress` / `scan.hit` / `scan.complete` / `scan.cancelled` lifecycle messages. Shared type definitions in `src/shared/types/ScanMessages.ts`.
- `AddSubnetModal` (inline entry from `ScanNetworkModal`) and `LargeSubnetWarningModal` — the latter fires when total scan size exceeds 2048 hosts and shows a per-subnet host-count breakdown so the user can narrow before committing. Both extend the native `<dialog>` `Modal` base class.
- `ScanProgressChip` bottom-anchored chip with four lifecycle states: `scanning` (live host counter), `draining` (in-flight probes finishing after user cancel), `complete` (summary counts), `cancelled`. Chip self-dismisses after the terminal state's grace period; re-attaches to in-progress scans on page reload via spectator snapshot replay.
- Empty-state card `"No devices connected."` spans the full row in the Connected Devices grid when no devices are attached — replaces the prior behavior of collapsing the section entirely (`4170955`, `e6ffbd8`).
- Subnet & CIDR cheat sheet page at `public/help/subnets.html` — opens in a new tab from `ScanNetworkModal` ("what's a subnet?" link) and inline from subnet-parser validation error messages (`cfc3d0e`, `944fac0`).
- Diagnostic scripts kept in-tree for future adb-protocol debugging (see `reference_wsscrcpy_debug_scripts.md`): `scripts/dump-cnxn.js` prints our CNXN packet hex for byte-level inspection; `scripts/test-probe-single.js <host> [port]` runs a one-shot single-connection CNXN probe against any IP and reports reply type (`AUTH` / `CNXN` / timeout) plus round-trip time.
- Config tuning knobs (env-var or `config.json` overrides): `scanConcurrency` (default `64`, max in-flight TCP connects), `scanTcpTimeoutMs` (default `300`, per-host connect timeout), `scanAdbConnectTimeoutMs` (default `5000`, CNXN handshake reply timeout), `scanProgressInterval` (default `10`, progress message cadence).
- Pencil edit icon on every user-added subnet row in `ScanNetworkModal`. Previously editing a subnet required delete + re-add; now a ✎ icon sits next to the × remove icon on each removable row. Clicking ✎ opens `AddSubnetModal` in edit mode with the current raw value pre-populated, the title changes to "Edit Subnet", and the primary button reads "save". Saving replaces the original row in place (same position), and the change is persisted to `localStorage`. Gateway-detected rows remain non-editable (no ✎ or × since they're auto-derived). Implementation: `SubnetRow` gained a stable monotonic `id: string` so edits target by ID rather than index/value, `removeRow(idx)` migrated to `removeRowById(id)`, new `updateUserRow(id, raw)` handles in-place replacement. `AddSubnetModal` accepts `mode: 'add' | 'edit'` + `initialValue?: string`; its submit callback was renamed from `onAdded` to `onSubmit` to reflect the mode-agnostic flow. 7 new tests cover both modes.

### Changed
- Network scan architecture rewritten from one-shot `POST` to streaming WebSocket. Server-side, a new `NetworkScanner` singleton (`src/server/services/NetworkScanner.ts`) owns a classic state machine — `idle → scanning → draining → idle` — with a bounded-concurrency pool (see `scanConcurrency`), cancel-drain semantics that let in-flight probes finish their current socket operation before the pool winds down, and mid-scan spectator snapshot replay so a late-joining client gets the current host count plus hits already found. `ScanMw` WebSocket middleware exposes the scanner at `/ws-scan` and registers at server startup (`607a972`, `49b2ffd`, `12a7237`, `437476e`, `5c15e29`).
- Probe path rewritten from `adb connect + getprop + disconnect` to a single-socket CNXN handshake probe in `AdbHandshakeProbe.ts` — the standard pattern used by `python-adb`, `tananaev/adblib`, WebADB, and `adbpy`. One TCP socket per host handles both port-5555 liveness and adb identification: TCP connect → send CNXN → read reply header → close. Devices answering with `CNXN`, `AUTH`, or any valid adb response header are logged as hits; everything else is rejected. On confirmed hits the scanner follows up with a one-shot `adb connect` + `getprop ro.serialno` to attach the real hardware serial (`0612472`, `bb0468d`, `20b9754`).
- MAC address resolution via system ARP cache (`MacResolver.ts`): `arp -a <ip>` on Windows, `ip neigh show to <ip>` on Linux. Fresh lookup after the probe's TCP traffic primes the kernel ARP table; caches results in-memory for the duration of each scan (`50bb239`).
- Device labels in `DeviceLabelStore` now persisted under both `ro.serialno` AND MAC address keys on `Connect` success. Scanner hit-lookup is `labelFor(mac) ?? labelFor(serial) ?? ''` — MAC-first catches TCP hits where serial isn't known until after follow-up connect, serial fallback catches mDNS hits where the mDNS record is serial-authoritative (`cb9e37b`, `64d9557`).
- mDNS display name normalized to `adb-{parsedSerial}` format — strips the TLS-connect suffix and re-adds the `adb-` prefix. Discovery-card display is now consistent between mDNS-sourced and TCP-sourced entries.
- `SubnetParser` hardens octet normalization, rejects multi-slash inputs, and drops an unreachable validation branch (`22f943a`); `SubnetDetector` adds `ifaceName` string hygiene and negative-mask corner-case tests (`5ad56ca`).
- CSS ellipsis + `title` tooltip on discovery card name/address — prevents overflow on long hostnames (`112933b`).
- `Scan network` button now opens the configuration dialog instead of immediately launching a mDNS-only scan. The legacy `POST /api/devices/scan` endpoint remains as a REST compatibility shim returning mDNS-only results with unchanged behavior — used by any external consumer that pre-dates `/ws-scan`.
- Subnet range parser accepts up to **65,536 addresses per range** (previously capped at a single `/24`, ~254 hosts) — matches the /16 ceiling already honored by CIDR input. When a range aligns to a subnet boundary (starts at `.0`, ends at `.255`), the parser auto-skips the network and broadcast addresses exactly as CIDR expansion does: `10.0.0.0-10.0.255.255` → 65,534 scannable hosts, identical to `10.0.0.0/16`. Partial-boundary ranges (e.g. `10.0.0.0-10.0.0.100`) skip only the .0 end; non-boundary ranges probe every address. Literal range size capped at 65,536; anything larger rejects with "maximum is 65,536 addresses (the size of a /16 CIDR block)". Tests cover both the cap and the network/broadcast skip in `src/server/__tests__/subnetParser.test.ts`.
- Scan progress chip now lives inside the `.discovery-info` empty-state card instead of the panel header, and replaces the default "Click scan network to find devices…" text while a scan runs. On chip dismiss — auto-hide after a terminal state (5s complete, 10s cancelled) or manual × — the default info text is restored. `ScanProgressChip` gained an `onDismiss?` callback and a `dismissed` flag to make dismiss idempotent; `NetworkDiscoveryPanel` captures the default info text at construction and restores it via `restoreInfoText()` (guards against clobbering a lingering error message). Chip is now `width: 100%` inside its slot with `min-height: 32px` and reduced vertical padding (6px → 4px), so all three lifecycle states (`scanning` with cancel button, `draining` with no button, `cancelled` with ×) render at identical external dimensions regardless of which child button is visible.

### Changed
- Scan-card layout restructured to accommodate a dismiss button without horizontal crowding. `.discovery-card-actions` now wraps (`flex-wrap: wrap`); `.discovery-name-input` takes full row 1 (`flex-basis: 100%`); Connect and close sit on row 2 with an 8px gap. Connect button restyled to the outlined-green convention used by the Connected Devices "turn on" button (transparent background, `#4ade80` border + text, 10% green hover) — previously used the solid-fill `.dep-update` class which was the only solid-fill button on the home page. Close button mirrors the same outlined treatment in `#f06c75` red, matching the Connected Devices "disconnect" button. `Connect` text lowercased to `connect` to match every sibling button (per the `feedback_ui_color_scheme` convention). Success-state class swap to `.dep-ok-btn` dropped — the 1500 ms `card.remove()` is the only success signal now, which avoids a one-frame solid-fill flash before the card disappears.
- All three home-page sections (Connected Devices, Available Network Devices, Dependencies) now wear a subtle 1px outline + 8px border-radius matching the inner card palette (`var(--device-border-color)`, falls back to `#444` dark / `#dee2e6` light). Border applied to `.home-section` (Available Network Devices, Dependencies) and `body.list #devices` (Connected Devices). The list-mode scope on `#devices` prevents the border from showing up on the stream-overlay variant (`body.stream #devices`) which is already position-absolute at full viewport.

### Added
- Dismiss button on each Available Network Devices card (`.discovery-dismiss-btn`). Clicking `close` removes the card from the DOM and deletes the address from `scanSessionHits` without interfering with the connect-success auto-remove path (`card.remove()` is idempotent). Lets the user clear unwanted results without rescanning. Rescan brings live devices back.

### Fixed
- Scan-card Connect button silent-failure regression. When `POST /api/devices/connect` returned `{success: false}` (e.g. `adb connect` reported "failed to connect to …: Connection refused"), `NetworkDiscoveryPanel.connectDevice()` re-enabled the button and dropped `result.message` on the floor — user saw the button briefly disable and re-enable ("blink and nothing") with zero diagnostic. Scan cards now render the server's message inline below the actions row in the same style as `discovery-manual-result` (new `.discovery-card-result` element, shared CSS). Matches the pattern `manualConnect` has used since e1bec55. Paired with the server-side logging fix below so we capture the actual adb output for post-mortem.
- CNXN probe aborted the TCP connection with RST instead of a clean FIN. `AdbHandshakeProbe.probeAdb()`'s `done()` unconditionally called `socket.destroy()` the moment the reply was parsed, which some embedded adbd stacks treat as an abort — they enter a brief cleanup window during which a subsequent real `adb connect` races with their state machine and silently fails. Symptom: immediately after a successful scan, clicking Connect on a :5555 device sometimes just "blinks" — adbd was still cleaning up from the probe. Fix: `done()` now uses `socket.end()` + a 250 ms safety-net `destroy()` when the probe confirmed `isAdb=true`; closed-port and timeout paths still use `destroy()` directly so they stay fast. Timer is `.unref()`-ed so the 250 ms window doesn't keep the event loop alive at shutdown.
- `POST /api/devices/connect` now logs every invocation with `adb connect` raw stdout, OK/FAIL status, and address — previously the server held the exact error (e.g. "failed to authenticate", "Connection refused", "unable to connect", etc.) in `result.trim()` and returned it to the client but never wrote it anywhere. Any future connect failure is now captured in `ws-scrcpy-web.log` for diagnosis. `DeviceDiscoveryApi`'s outer catch-all also logs `req.method` / `req.url` / message on any thrown exception from the handler.
- Scanner regression on older embedded adbd stacks — the SM-T550 tablet at `192.168.86.231:5555` (Galaxy Tab A 9.7 running Android 7.1.1) silently dropped every second TCP connect to port 5555. The previous probe used two back-to-back sockets per host (`adb connect` for liveness, then `adb disconnect` after), and the tablet's embedded adbd stack rejected the second one without logging anything. Tablet surfaced as "offline" despite being reachable and wireless-debugging-enabled. Rewrote as the single-connection CNXN probe described in **Changed** above — one socket handles everything. Modern devices were working by accident; both paths now use the single-socket flow that every serious adb-protocol library uses. Empirically confirmed via `scripts/test-probe-single.js` — tablet now replies `AUTH` in ~290 ms on single-connect (`c3058c1`).
- CNXN packet byte-level parity with real adb. Earlier iterations used plausible-but-wrong values that modern adbd tolerated but stricter parsers rejected: version was guessed — now hardcoded to `0x01000001` matching AOSP `adb.h`; `max_data` was `0x00001000` (4 KiB) — now `0x00100000` (1 MiB), matching real adb and avoiding fragmentation of the 302-byte banner; banner was bare `host::` — now the full `host::features=shell_v2,cmd,stat_v2,…` feature list a real v3.x client sends; `data_check` was implemented as CRC32 — now a simple byte-sum per the protocol. **The field is named `data_crc32` in the message struct — this is a well-documented historical misnomer going back to protocol v1** (see `python-adb/adb_protocol.py`, `tananaev/adblib`, the WebADB connection write-up). Sum of payload bytes mod 2^32, no overflow handling. Also dropped the trailing null byte from the banner payload — real adb doesn't send one and some embedded parsers reject it (`7e2e275`, `070ec60`).
- Windows multi-homed default-route garbage. On machines with multiple physical adapters plus a static-IP interface (common on dev boxes), `route print` emits a synthetic `On-link` row and a bogus `0.0.0.0` metric-0 default for every interface. The gateway detector was picking the first row and scanning a `/16` of an interface we weren't actually using. Now filters `On-link` and `0.0.0.0` gateway rows, then picks the lowest-metric real default. Three-level fallback cascade: `route print` (Windows) / `ip route` (Linux) → RFC1918 interface enumeration → null (`431bf97`).
- ES2022 class-field clobber in new modals. `ScanNetworkModal`, `AddSubnetModal`, and `LargeSubnetWarningModal` assigned DOM refs in `buildBody()` / `buildFooter()` called from the `Modal` base-class constructor, but with `useDefineForClassFields: true` (our `tsconfig.json` default) the subclass's class-field initializers run AFTER `super()` returns, overwriting those refs with `undefined`. Fix: defer body/footer content assignment via `queueMicrotask` so the write happens after field initializers have run. Pattern captured in `feedback_es2022_class_fields` memory so future modal authors don't re-hit it (`58d1b61`).
- WebSocket listener leak on abnormal `/ws-scan` close (`fa9b02f`). The middleware attached a `message` handler cleared only on clean close; abnormal paths (client network drop, tab close mid-scan) left the handler registered against the closed socket. Now cleared on any close transition.
- Progress chip ghost state on abnormal WS close before terminal message (`374b6e4`). If the socket closed before a `scan.complete` / `scan.cancelled` arrived, the chip would stick at "scanning" indefinitely. Chip now dismisses on `ws.close` as a fallback.
- Nagle delay on CNXN probe (`934149a`). `setNoDelay(true)` on the probe socket before writing CNXN — without it the 6-byte header and payload were being coalesced by the client-side Nagle algorithm on some stacks, adding ~40 ms per probe (untenable at `scanConcurrency=64` across a `/24`). Handshake timeout also bumped from 2 s to 5 s so slow embedded adbd stacks get enough time to negotiate `AUTH`.
- Cheat-sheet link clipboard copy, shared WS-path constant, deduped mDNS helper utility (`96f2664`).
- Cheat-sheet back-link leaked new tabs. The `← Back to ws-scrcpy-web` link at the bottom of `public/help/subnets.html` used `href="../"`, which navigated the tab opened via `target="_blank"` back to the app home rather than closing it — repeated cheat-sheet visits piled up stale tabs. Now uses `window.close()` with the same href as fallback, and the link text reads `← Close tab` to match the behavior.
- Scan progress chip flashed "Finishing active scans…" for one frame on cancel, then reverted to "Scanning network…" for about a second before landing on "Scan cancelled". Two compounding causes: (1) the drain phase completes in under the `scanTcpTimeoutMs` budget (~300 ms) so the drain label was imperceptible; (2) server-side `scan.progress` messages kept arriving while in-flight probes wound down, and each one called `setScanning` which overwrote the drain label back to the running-scan text. Fix: `ScanProgressChip` now tracks its own state and holds the draining label for a minimum **1200 ms** before transitioning to `cancelled`, AND `setScanning` is a no-op once the chip has left the `scanning` state so stale progress events can't resurrect the scanning label during drain or after a terminal transition.
- Modal-on-modal readability — opening `LargeSubnetWarningModal` on top of `ScanNetworkModal` (triggered by scans >2048 hosts) stacked two translucent glassmorphism frames, and the compound transparency made the inner modal's text hard to read over the lower modal's glass. Fix: CSS `:has()` rule in `src/style/modal.css` detects when two `dialog.modal[open]` siblings exist and makes the topmost (`:last-of-type`) use a fully opaque frame (`rgb(30, 35, 45)` dark / `rgb(245, 248, 252)` light). Single-modal presentations retain the glass effect. No `Modal` base-class changes — pure CSS.

### Changed
- Home-page section spacing and outline treatment tuned. Top-of-page to first section border, between Connected Devices and Available Network Devices, and between Available Network Devices and Dependencies — all three gaps are now a consistent **35px**. Outline width bumped `1px → 1.5px`. Introduced a dedicated `--section-border-color` CSS variable (dark `#5a5a5a`, light `#8f959d`) so section frames stay brighter than the inner card outlines (`--device-border-color`, dark `#444` / light `#c0c6cc`) — gives a clear hierarchy without making either level feel like a hard line. Light-mode `--device-border-color` and `--button-border-color` also darkened from `#dee2e6` → `#c0c6cc` since the previous value was nearly invisible on the white `--main-bg-color`. Discovery cards gained a 15px `margin-bottom` on `.discovery-results:not(:empty)` so they stop butting up against the "Click scan network…" info card; the empty-state layout is unchanged when no cards are present.
- Scan-card action row layout restructured. `.discovery-card-actions` now wraps; `.discovery-name-input` takes full row 1 (`flex-basis: 100%`); `connect` + `close` sit on row 2 with an 8px gap, so a long device name in the input can't squeeze the buttons off the right edge.
- Manual-add connect button in the Available Network Devices panel now uses the same outlined `.discovery-connect-btn` styling as the scan-card `connect` button (was still carrying the old solid-fill `dep-update` class). Both paths through the section are now visually consistent: outlined green border + text on transparent, 10% green hover fill.
- Dependency panel text sizes bumped to match the rest of the home page — `.dep-table` was `0.9rem` (≈12.6px at 14px root) against 13–14px everywhere else on the home page; tables now render at `1rem` (14px) with `.dep-version` / `.dep-description` at `0.93rem` (≈13px) and `.dep-paired` / `.dep-badge` at `0.86rem` (≈12px). `.dep-restart-prompt`, `.dep-restarting p`, and `.dep-error-msg` all follow the same bump.
- Dependency status badges (Up to date / Update available / Checking… / Updating… / Error / Unknown) converted from filled pills to outlined pills. Single shared `.dep-badge` rule adds `border: 0.5px solid currentColor; background: transparent` so each status's text color is the border color automatically, no per-class overrides needed. Matches the outlined aesthetic of every other action button on the page. Status colors themselves are now theme-aware via new `--success-color`, `--warning-color`, `--info-color`, `--danger-color` variables (dark `#4ade80 / #fbbf24 / #60a5fa / #f06c75`; light `#15803d / #b45309 / #0969da / #b91c1c`) — the previous values were all bright single-token colors that rendered nearly invisible on white (e.g. `#4ade80` on white has a 1.98:1 contrast ratio — below even the AA-large threshold of 3:1).
- Modal dark-theme frame color shifted from the bluish `rgb(30, 35, 45)` to the neutral `rgb(42, 42, 42)` that matches the app's `--main-bg-color: #2a2a2a`. Both the translucent single-modal frame (`rgba(42, 42, 42, 0.85)`) and the opaque stacked-modal top layer got the same treatment, so nested modal chains (e.g. `LargeSubnetWarningModal` on top of `ScanNetworkModal`) no longer stand out in bluish tone against the rest of the dark UI.
- Subnet cheat sheet (`public/help/subnets.html`) now follows the app's theme. An inline script at the top of `<head>` reads the main app's `ws-scrcpy-web-theme` localStorage key (same key `ThemeToggle.ts` writes) and sets `data-theme` on `<html>` before first paint, so there's no flash. The cheat sheet's local CSS variables are split into `:root, [data-theme="dark"] { … }` and `[data-theme="light"] { … }` blocks, with the dark palette updated to match the app (neutral grey `#2a2a2a` / `#1f1f1f` / `#e0e0e0` / `#a8a8a8` / `#444`, link color `hsl(218, 63%, 70%)`) — the old palette was a GitHub-style bluish-black that didn't fit the rest of the app. Light palette uses GitHub-ish white (`#ffffff` / `#f6f8fa` / `#212529` / `#495057` / `#d0d7de`, link `#0969da`).

### Added
- Theme design tokens added to `src/style/app.css`, available in both themes:
  - `--success-color` / `--success-hover-bg` — outlined green buttons (`.discovery-connect-btn`, `.sleep-wake-btn.state-off`).
  - `--danger-color` / `--danger-hover-bg` — outlined red buttons (`.discovery-dismiss-btn`, `.disconnect-btn`, `.sleep-wake-btn.state-on`).
  - `--warning-color` / `--info-color` — dependency status badges (`.dep-warn`, `.dep-info`).
  - `--section-border-color` — subtle outline around each home-page section (`body.list #devices`, `.home-section`).
  Values chosen to pass WCAG AA on the respective theme's bg: dark-mode tokens keep the existing bright shades (`#4ade80` / `#f06c75` / `#fbbf24` / `#60a5fa`) readable on `#2a2a2a`; light-mode tokens darken to `#15803d` / `#b91c1c` / `#b45309` / `#0969da` for readability on `#ffffff`.

### Fixed
- Three widely-used theme CSS variables were never declared, so everywhere they were referenced the browser silently fell back to the hardcoded values in their `var(…, fallback)` clauses — which were light colors picked for dark backgrounds. On the light theme these showed up as faded grey on white, borderline-unreadable text. Fixes:
  - `var(--text, #e6edf3)` → `var(--text-color)` in `ScanProgressChip.ts` (chip label, completely unreadable on white `main-bg-color`).
  - `var(--muted, #8b949e)` → `var(--text-color-light)` in `ScanProgressChip.ts` (chip dismiss ×), `AddSubnetModal.ts` (help line), `LargeSubnetWarningModal.ts` (advice line), `ScanNetworkModal.ts` (cheat sheet footer link).
  - `var(--text-color-secondary, #888)` → `var(--text-color-light)` in 5 places in `src/style/home.css` (empty-state card, discovery-card address, two input placeholders, discovery-manual-close ×). `--text-color-secondary` has never been defined in any theme block; every one of these rendered as `#888` on white (3.54:1 contrast, fails AA for normal text).
  - `.dep-unknown` in `src/style/dependencies.css` had a hardcoded `color: #888` with no theme awareness — fixed to `var(--text-color-light)`.
- `--text-color-light` itself rebalanced per theme for readability: light mode `#6c757d → #495057` (contrast on `#ffffff` jumps 4.54:1 → 8.5:1); dark mode `#888888 → #a8a8a8` (contrast on `#2a2a2a` improves 4.48:1 → 6.3:1, more comfortable for longer secondary text).
- Light-mode green and red for outlined action buttons were hard to see on white. `.discovery-connect-btn`, `.discovery-dismiss-btn`, `.disconnect-btn`, `.sleep-wake-btn.state-off` / `.state-on` all had hardcoded `#4ade80` / `#f06c75` that produced ~2:1 and ~3:1 contrast ratios on `#ffffff` respectively. Switched to the new theme-aware `--success-color` / `--danger-color` tokens (dark mode keeps the bright shades; light mode gets the darker `#15803d` / `#b91c1c` for AA contrast).

### Performance
- Main frontend bundle (`bundle.js`) dropped from **525 KiB → 162 KiB** (-69%) by converting static imports of `ShellModal`, `ListFilesModal`, and `ConnectModal` to dynamic `await import()` at the click-handler call sites in `DeviceTracker.ts` and `ConfigureScrcpy.ts`. xterm.js (the heaviest dep at ~326 KiB) now loads only when the user clicks the `shell` button — home page initial-paint no longer pays for it. `ConnectModal` and `ListFilesModal` split out similarly. Webpack `performance` budget tuned to 400 KiB asset / 500 KiB entrypoint on both frontend and library configs — the 244 KiB default targets content sites, not a tool app with an embedded streaming stack + terminal + file manager. All 5 webpack configs now build with zero warnings.

### Added
- Fast-path probe + session-start optimizations for older Android. On SDK < 28, `DeviceProbe` now uses scrcpy-server's `list_encoders=true` mode (authoritative MediaCodecList enumeration) instead of `dumpsys media.player` — which on these devices emits no encoder entries and takes ~68s on the SM-T550. Modern devices (SDK >= 28) keep the fast dumpsys path. `ScrcpyConnection.start()` now skips the `adb push` of scrcpy-server when the remote copy is already present with matching size (keeps Android's dexopt cache warm between sessions), reads the device SDK from ControlCenter's descriptor before falling back to an adb-shell round-trip, and tightens the forward-tunnel retry/inter-socket delays. Stacked effect on a rebooted SM-T550 (API 25): first stream cold-starts in ~20-25s (dexopt on first load), subsequent streams in ~3-4s.
- SDK-gated tunnel mode + cleanup flag + dummy-byte handshake consumption in `ScrcpyConnection.start()`. Android pre-9 (SDK < 28) devices — e.g. the Samsung Galaxy Tab A on 7.1.1 — don't support `adb reverse` over WiFi (older Android adbd reports "more than one device/emulator" regardless of `-s`). The stream now uses `tunnel_forward=true` with host-initiated connections on those devices, and the standard reverse tunnel elsewhere. Audio is also forced off on SDK < 30 because scrcpy audio forwarding requires Android 11+. Finally, `cleanup=false` is set on SDK < 28 because scrcpy-server's default self-delete of its own JAR at startup races class loading on pre-8 Android — app_process aborts with `ClassNotFoundException` before the server classes fully resolve. Keeping the JAR around until explicit teardown fixes the abort. Lastly, in tunnel-forward mode scrcpy-server writes a single 0x00 handshake byte on each of the three sockets (video, audio, control) before real traffic — a flush for adb's forward buffer that reverse-mode doesn't emit. Our client now consumes and discards that byte on each socket before calling parseMetadata, otherwise every subsequent field is off by one and the decoder fails silently.
- **Manually add device** button next to Scan Network in the Available Network Devices panel. Opens an inline form (IP, port defaulting to 5555, optional name) for adding devices that don't advertise over mDNS — typically Android pre-11 tablets with wireless debugging enabled over a fixed port. Reuses the existing `/api/devices/connect` endpoint. When a name is provided, the server looks up `ro.serialno` after the connect succeeds and persists the label keyed by hardware serial.
- Auto-detect Android device kind (phone / tablet / tv) on connect using `ro.build.characteristics`, `pm has-feature android.software.leanback`, and `smallestWidthDp >= 600`. Surfaces as a small icon badge inside the connect pill on each device card.
- Stream toolbar's D-pad/Touch input-mode toggle now initializes to D-pad for TVs and Touch for phones/tablets. Toggle still works both directions after stream start.
- `deviceKind?: 'phone' | 'tablet' | 'tv'` optional field on `GoogDeviceDescriptor` and on `StartStreamOptions` (public `startStream` API).
- `SECURITY.md` now points to GitHub Security Advisories for private vulnerability reporting
- `CONTRIBUTING.md` with setup, style guide, and PR expectations
- `CHANGELOG.md` following Keep a Changelog format
- `repository`, `author`, `bugs`, `homepage`, and `keywords` fields in `package.json`
- `CommandControlMessage.createGetClipboardCommand(copyKey)` factory with `COPY_KEY_NONE` / `COPY_KEY_COPY` / `COPY_KEY_CUT` constants

### Changed
- Personal filesystem paths scrubbed from historical plan documents in `docs/plans/` and `docs/superpowers/plans/`
- Stream layout in `ws-scrcpy.css` promoted to flex-row (video | toolbar) with CSS grid for canvas-layer stacking — replaces legacy `float: right` + `position: absolute` layout. Redundant `dialog.connect-modal` layout overrides in `modal.css` deleted; modal keeps a targeted canvas-layer cap to fit inside the `95vw × 90vh` frame
- Home page section headings ("Connected Devices", "Available Network Devices", "Dependencies") now render at a consistent `18px / 600` weight. `#devices .tracker-name` was `font-size: larger; font-weight: bolder` and `.dep-header h2` was `1.1rem` — both now inherit / match `.home-section h2`
- `.dep-btn` hover (scan network, check for updates) replaced `opacity: 0.8` with `background-color: var(--device-list-hover-color)` — matches the blue-tint hover used on device card overlay buttons (`.action-button`)
- Device card button padding (`.action-button`, `div.desc-block a`) dropped from `6px 12px` to `0.3rem 0.75rem` so the "connect" / shell / list-files pills match the vertical size of `.dep-btn` (scan network, check for updates) on the same page
- `.home-section` now sets `font-family: monospace` so the "Available Network Devices" and "Dependencies" panels match the monospace styling used by the device list section above them; previously they were rendering in the browser-default (serif) font
- Device card overlay buttons laid out as a 2×2 grid filled column-first: left column `[shell][list files]`, right column `[connect][config stream]`. Buttons are vertically centered and left-aligned in their cells. `.services` swapped from `flex-wrap` to `display: grid; grid-template-columns: 1fr 1fr; grid-auto-flow: column; align-items: center; justify-items: start`; the `.services-break` hack div is gone. `.services-label` spans both columns via `grid-column: 1 / -1; grid-row: 1` so it stays anchored at the top regardless of fill direction
- Services label renamed `opens in overlay` → `opens in modal` (matches the actual UI — these are dialog-based modals, not overlays). Stream-entry button text renamed `configure stream` → `config stream` for brevity
- Device card button typography normalized: `disconnect` and `turn on`/`turn off` were pinned to `font-size: 12px` while every other button on the page uses `var(--font-size)` (14px) — now all bump to 14px. `font-family: monospace` added explicitly to `#devices .device-list button` and `.desc-block a` / `.desc-block .action-button` so `<button>` elements (which don't inherit font-family from their parent in most browsers by default) don't fall back to the UA default sans-serif; catches `config stream` and the `list files` anchor that were rendering in different fonts than the rest of the card
- All `font-family: monospace` call sites now route through a new `--font-mono` CSS variable in `app.css`: `Consolas, 'Segoe UI Mono', 'SF Mono', Menlo, 'Liberation Mono', 'DejaVu Sans Mono', monospace`. The bare `monospace` generic keyword resolves to the browser/OS's configured monospace default, which on Chromium/Windows is often **Courier New** (slab-serif). The new stack explicitly prefers clean sans-serif monospace fonts per-platform and only falls back to the generic keyword as a last resort
- Disconnect / turn on / turn off / scan-connect buttons no longer swap label text on click (`'disconnecting...'`, `'turning on...'`, `'Connecting...'`/`'Connected'`/`'Failed'`/`'Error'`). With the new 14px font the longer transient labels overflowed their fixed-width buttons. Click handlers now only toggle `disabled` state — CSS `:disabled` rules already apply `opacity: 0.5` for visual feedback. Sleep/wake still updates its label after the response resolves (because the label reflects the device's awake state, not the in-flight action)

### Fixed
- Device-kind detection previously skipped the whole classifier when any one of its four ADB shell probes exited non-zero. Observed on Pixel 9: `pm has-feature android.software.leanback` exits `1` when the feature is absent, and the shared `try/catch` around the `Promise.all` scuttled the pass entirely — phone badge never appeared. Each probe now has its own `.catch(() => '')`, so an ignorable exit produces an empty string and the classifier degrades gracefully (empty leanback is not `'true'` → not TV; size/density still parse → phone/tablet split works).
- UHID virtual keyboard / mouse toggle was silently desyncing the control stream against scrcpy v3.3.4. `UhidCreateMessage` was encoding the pre-Dec-2024 wire format — missing `vendorId` (2 bytes) and `productId` (2 bytes) between `id` and `name`, and writing `nameLen` as 2 bytes instead of 1. Scrcpy commit `27a5934a` (2024-12-07, "Define UHID vendorId and productId from the client") added the two new fields, and `ControlMessageReader.parseUhidCreate` uses `parseString(1)` for the name (1-byte length). Fix: added `vendorId` / `productId` to the encoder (both set to 0 to match scrcpy's own `app/src/uhid/{keyboard,mouse}_uhid.c`) and narrowed `nameLen` to `writeUInt8`. Added a byte-layout test that asserts the v3.3.4 wire format so a future drift fails loudly. Found during the Track B protocol audit — all other message types (KEYCODE, TEXT, TOUCH, SCROLL, GET/SET_CLIPBOARD, SET_SCREEN_POWER_MODE, UHID_INPUT, UHID_DESTROY) already match v3.3.4.
- Configure Stream dialog no longer lets the user pair a video codec with a mismatched encoder (e.g. `h264` + HEVC encoder), which crashed scrcpy-server on the device when MediaCodec rejected the configuration. Encoder dropdown is now filtered by the selected video codec using the same `.avc.` / `.hevc.` / `.av1.` name patterns that `detectVideoCodecs()` uses; a `change` listener on the codec dropdown re-filters encoders live. When the previous encoder is not valid for the new codec (or none was picked), the dropdown auto-selects the first matching encoder rather than falling back to `''` — Android's MediaCodec typically orders HW encoders first, so this ends up selecting a HW encoder on most devices
- Stream library stylesheet (`ws-scrcpy.css`) now self-contains theme variables so `/embed.html` renders back/home/overview buttons correctly in light mode
- `/embed.html` no longer shows black dead space to the right of the video or below it — stream now fills the viewport via `body[data-embed-entry]` opt-in flex layout. `StreamClientScrcpy.getMaxSize()` now reads `window.innerWidth/Height` directly (not `document.body`), avoiding a zero-sized measurement at mount time when placed inside a `fit-content` modal frame
- Clipboard GET toolbar button sent the wrong control-message size (1 byte instead of 2 — missing the required `copy_key` byte per scrcpy v3.3.4's `ControlMessageReader.parseGetClipboard`). Alternating GET and SET presses silently misaligned the control stream by one byte, causing scrcpy-server to parse garbage and close the session. Now sends `[type=8, copy_key=0]` per protocol
- Portrait-aspect devices (e.g. Pixel 9, 952×2128) no longer render past the bottom of the viewport in `/embed.html`. The canvas was rendering at its intrinsic size because the player's `calculateScreenInfoForBounds` is gated on `resizeVideoToBounds` (false on every subclass — effectively dead code), so CSS alone was responsible for fit-to-viewport. The legacy viewport-based `max-width: calc(100vw - 3.715rem); max-height: 100vh` on `.video-layer` and `.touch-layer` was lost in the layout refactor; restored as the default so canvas scales down to viewport while preserving aspect. Modal's tighter `calc(95vw - …)` / `calc(90vh - …)` still wins via specificity.
- Embed layout: video + toolbar now right-aligned as a pair (matches legacy `float: right` look). Previously the video cell was stretched across the flex row with `flex: 1`, centering the canvas in the middle and leaving the toolbar marooned at the far right — obvious on phone aspects where the centered canvas was narrow. Now `body[data-embed-entry] .device-view` uses `justify-content: flex-end`; the video cell stays content-sized so the toolbar sits flush against the mirror's right edge.
- `/embed.html` now parses the `deviceKind` URL parameter. `StartStreamOptions.deviceKind` has existed since its addition alongside the device-kind auto-detection feature (see Added), but `embed-entry.ts`'s `parseEmbedParams` was silently dropping the param on the URL side — iframe consumers like Control Menu's `ScrcpyMirror` component could pass `&deviceKind=phone` without effect. Now validated against the closed set `phone | tablet | tv` (anything else is ignored, keeping forward-compat). Test coverage added in `embedEntry.test.ts`.

### Added
- Logger coverage in `DependencyManager` at medium granularity (update start/complete, checkAll aggregate, restart requested, errors).
- Node auto-update gating against the node-pty prebuilt manifest (Option D): `nodejs.checkLatest` only offers LTS versions we have prebuilts for; silently skips newer majors until the matrix catches up.
- Launcher scripts (`start.cmd`, `start.sh`) now also loop on process exit code 75, in addition to the `.restart` marker.

### Changed
- `DependencyManager.requestRestart` now writes the `.restart` marker at `<depsPath>/.restart` (was `dirname(depsPath)/.restart`) and exits with code 75 (was 0). Launcher scripts updated to read the marker from `$DEPS_PATH/.restart` and also loop on exit code 75. This unbreaks the marker path under Velopack's `<installFolder>/current/` layout and lets supervisors (systemd, Docker restart policies) distinguish intentional restart from crash.
- `Config.dependenciesPath` resolution is now strict: `DEPS_PATH` environment variable wins, then `config.json` `dependenciesPath`, then a dev-only fallback that triggers only when a sibling `package.json` is present. Production deployments (Velopack, Docker) must set `DEPS_PATH`. A clear startup error names the env var and config key if no source resolves.
- Never-auto-downgrade rule: when the (possibly filtered) latest version is older than the installed version, status stays `UpToDate` with an explanatory INFO log.

### Added
- `DependencyManager.autoInstallMissing()` — first-run bootstrap primitive that installs any managed dep with `installedVersion === null && latestVersion !== null`. Called once after the startup `checkAll` completes.
- `POST /api/dependencies/retry-install` endpoint — re-runs `checkAll` + `autoInstallMissing` and returns a summary of installed / still-missing / errored deps. Used by the first-run banner's Retry button.
- Home-page first-run banner (`FirstRunBanner`) — renders when any dep is in `Error` state or `Unknown` with null `installedVersion`. Offers a Retry button for offline-at-first-boot recovery.

### Changed
- Launcher scripts (`start.cmd`, `start.sh`) now probe `dependencies/node/` first and fall back to `seed/node/` (the Velopack-bundled location) when the dep-managed copy is absent. Supports fresh Velopack installs out of the box.

### Fixed
- First-run banner uses hardcoded theme-independent amber background + near-black text (contrast ~11:1) instead of the project's `--warning-color` / `--text-color-light` theme tokens. Deliberate deviation from the `reference_wsscrcpy_theme_vars` convention: those tokens resolved to ~1.5:1 contrast (well below WCAG AA) in both dark and light themes because the token pairing wasn't designed for warning-surface use. A warning banner needs to stand out regardless of theme, so fixed colors are correct here.

## [1.0.0] - 2026-04-17

First public release. Browser-based Android screen mirroring rebuilt from the ground up on vanilla scrcpy v3.x with a modernized Node.js + TypeScript stack.

### Added

**Stream API + embed mode** (this release's headline)
- Public `WsScrcpy.startStream(container, deviceId, options)` library shipped as UMD (`ws-scrcpy.umd.js`) and ES module (`ws-scrcpy.esm.js`) with bundled TypeScript types (`ws-scrcpy.d.ts`)
- `/embed.html?device=<udid>` thin wrapper for iframe consumers; transparent background, auto-connect, full toolbar
- `StreamHandle` with idempotent `stop()`, `isConnected`, `deviceId`
- `onConnect` / `onDisconnect` / `onError` lifecycle callbacks with typed payloads
- Full URL parameter surface (`host`, `port`, `secure`, `pathname`, `codec`, `encoder`, `bitrate`, `maxFps`, `maxSize`, `audio`, `keyboard`)

**Modal system**
- Native HTML `<dialog>` base class (`Modal`) with glassmorphism styling, `@starting-style` transitions, and `addHeaderButton()` helper
- `ConfigureScrcpy`, `ShellModal`, `ConnectModal`, `ListFilesModal` all extend the base class
- Device labels displayed in modal headers

**File browser** (`ListFilesModal`)
- Sticky header, reserved actions column, SVG hover icons that scale with size picker, sortable columns, breadcrumb navigation, bulk selection, drag-and-drop upload, download with progress, client-side filter

**Input**
- UHID keyboard + mouse via USB HID report descriptors (pointer lock)
- D-pad / Touch input mode toggle (D-pad default for TV apps, fire-then-debounce for scroll wheel)
- Scroll wheel with i16fp encoding (`sc_float_to_i16fp`) and latent-stream-tuned normalization
- Clipboard toolbar buttons (GET device→host, SET host→device) — modernized from legacy MoreBox textarea flow

**Codecs**
- Multi-codec video: H.264, H.265 (HEVC), AV1 with smart auto-selection (H.265 preferred, falls back to H.264 for Firefox)
- Multi-codec audio: Opus, AAC, FLAC, raw PCM via WebCodecs `AudioDecoder` + `AudioWorklet`
- HEVC SPS parser with RBSP stripping, AV1 config record parser
- Edge H.265 rendering fix: 8-arg `drawImage` using full coded rect as source (Edge reports display dims ≠ coded dims)

**Device management**
- Connected-devices card grid with live WebSocket updates
- Network scan via `adb mdns services` with one-click connect
- Device labels persisted to `device-labels.json`, keyed by `ro.serialno`
- Per-card sleep/wake toggle with server-side polling (`dumpsys power`, 5s loop, `Promise.all` concurrency)
- Disconnect button for network-connected devices

**Deployment**
- Self-contained folder layout: `dependencies/node/`, `dependencies/adb/`, `start.cmd` / `start.sh` launcher scripts
- In-app updater for Node.js + node-pty (paired), ADB platform-tools, scrcpy-server
- Windows file-locking workaround: rename running `node.exe`, write `.restart` marker, launcher relaunches
- Dark/light theme toggle with localStorage persistence

**Server**
- Tagged logger (`Logger.for('Tag')`) replaces all raw `console.log`; tees to `ws-scrcpy-web.log` with ISO timestamps, 5MB rotation
- `uncaughtException` + `unhandledRejection` handlers log to file before exit
- Crash-safe WebSocket close (readyState guard, 123-byte reason truncation)
- Vanilla scrcpy-server v3.3.4 binary; no Java patching

**API endpoints**
- `GET /api/dependencies/*` — updater status and operations
- `GET /api/devices/labels` / `PUT /api/devices/labels`
- `POST /api/devices/scan` — mDNS discovery
- `POST /api/devices/connect` / `POST /api/devices/disconnect`
- `POST /api/devices/files/*` — file browser operations including delete

**Quality stats overlay**
- Top-left HUD shows resolution, video codec, encoder name, bitrate, FPS counters; font scales with canvas resolution
- Toolbar bar-chart button toggles stats visibility
- Server echoes encoder in session metadata

**Tests**
- Vitest suite for control messages, binary readers/writers, multiplexer, codec configs, device labels
- 87 tests passing across the final release

### Changed

- Dependencies overhaul: Node 24 LTS, TypeScript 6, Biome 2, webpack 5, node-pty 1.1.0, xterm 6.x
- Runtime dependencies reduced to 2 total: `ws`, `node-pty`
- Control message protocol: `ScrollControlMessage` now 20-byte int16 (not 25-byte int32); `TouchControlMessage` payload corrected to 31 bytes
- Default keyboard: ON at stream start
- Default FPS: 15 (tuned for latent network streams)
- Default encoder: auto-selects hardware HEVC (`c2.mtk.hevc.encoder`, Qualcomm or Exynos equivalents)
- Home page centered at max-width 1800px (5 cards on 4K)
- Toolbar icons centered via SVG sizing; vertical spacing increased

### Removed

- iOS support, Chrome DevTools proxy, WASM decoder fallbacks, vendor decoder shims (~6,500 lines deleted)
- `adbkit`, Express, YAML, ESLint, path-browserify (replaced by own implementations)
- `GoogMoreBox` (383 lines) — clipboard flow replaced by toolbar buttons
- `#!action=stream` URL hash routing
- `?embed=true` URL parameter and all `body.embed` CSS rules
- Patched `scrcpy-server.jar` — project now uses unmodified Genymobile binaries

### Fixed

- Edge WebCodecs H.265 displayWidth/codedWidth mismatch causing blurry or clipped frames
- Firefox `VideoDecoder.isConfigSupported` falsely rejecting `avc1.42E01E` — H.264 now skips the check
- Mouse click freeze after stream-quality refresh (race: old demuxer's async `onclose` fired after `isRefreshing` reset)
- Stale device cards persisting across disconnects (ControlCenter + client-side `updateDescriptor` both now remove disconnected devices)
- Scan Network missed plain `_adb._tcp` services (filter was restricted to `_adb-tls-connect`)
- `RemoteShell` crash from `ws.send()` on closed socket (readyState guard)
- `AdbUtils.ts` and `RemoteShell.ts` cross-platform fixes (hardcoded `'adb'` → `Config.adbPath`, `env.PWD` → `process.cwd()`)

### Security

- WebSocket close reason truncated to 123-byte spec limit with try/catch — offline devices no longer crash the Node process
